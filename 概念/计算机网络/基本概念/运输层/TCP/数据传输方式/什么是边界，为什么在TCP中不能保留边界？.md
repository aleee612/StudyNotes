---
aliases:
  - TCP为什么不保留消息边界
  - TCP消息边界
---

### TCP 消息边界

- [[TCP]] 面向字节流，不保留应用每次 `send()` 的边界；
- 一次发送可能被多次读取，多次发送也可能被一次读取；
- TCP 报文段边界不等于应用消息边界；
- 这由 TCP 接口语义决定，不是“可靠传输必然没有边界”。

#### 应用定界

- 固定长度；
- 长度字段 + 正文；
- 分隔符；
- 协议自带帧结构。

[[UDP]] 面向报文，保留数据报边界，但默认不保证可靠、有序。相关：[[粘包问题]]、[[字节流传输]]。

