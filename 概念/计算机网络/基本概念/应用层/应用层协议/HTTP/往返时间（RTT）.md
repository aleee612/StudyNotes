---
aliases:
  - 往返时间
  - RTT
---

### RTT

RTT：分组从发送端到接收端再返回发送端的时间，包含传播、发送、排队和处理时延。

经典非持续 HTTP 获取一个小对象：

$$
T\approx 2RTT+对象发送时延
$$

其中 1 RTT 建立 TCP，1 RTT 发送请求并收到响应首字节。DNS、TLS 等额外过程另计。

相关：[[Web和HTTP]]、[[持续&非持续连接]]。

