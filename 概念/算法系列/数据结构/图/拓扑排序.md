> 拓扑排序：保证顺应边的顺序即可，拓扑排序可以有多个结果
#### 核心思想：
1. 计算所有节点的入度、出度
2. 将所有入度为0的节点删去，并清除其影响：即与该节点相连的节点入度变为0
3. 循环2步骤，直至所有节点都被删去
- 为达成步骤2，需要使用邻接表记录整个图结构
### 题目练习
##### leetcode 210
![[Pasted image 20260317155623.png]]
![[Pasted image 20260317155639.png]]
```cpp
class Solution {
public:
    vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
        vector<vector<int>> map(numCourses);
        int pre[2000] = {0};
        for (int i = 0; i < prerequisites.size(); i++) {
            map[prerequisites[i][1]].push_back(prerequisites[i][0]);
            pre[prerequisites[i][0]]++;
        }
        vector<int> ans;
        int q[2000];
        int l = 0, r = 0;
        for (int i = 0; i < numCourses; i++) {
            if (pre[i] == 0) { // 入度为0
                q[r++] = i;    // 入队列
            }
        }
        while (l < r) { // 队列不为空时
            int cur = q[l];
            ans.push_back(q[l]);
            for (int j = 0; j < map[cur].size(); j++) {
                pre[map[cur][j]]--; // 对应的下一位的入度减一
                if (pre[map[cur][j]] == 0) { // 减一后当前元素对应的入度若为0
                    q[r++] = map[cur][j]; // 就入栈
                }
            }
            l++; // 清理完当前元素后，将该元素剔除
        }
        if (ans.size() != numCourses) return {};
        return ans;
    }
};
```
- 注意事项：while循环中不使用for来进行对队列的遍历，而是用对于 `l` 的控制来实现
- 老问题，需注意元素是值还是下标
##### 洛谷 p4017

##### leetcode 2127